<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lista de Tarefas</title>
</head>
<body>
  <h1>Lista de Tarefas</h1> 
  <a href="/tasks/criar">Criar Tarefa</a>
  <ul>
    <% tasks.forEach(task => { %>
      <li>
        <%= task.titulo %> - <%= task.descricao %> - <%= task.completada ? 'Completada' : 'Não Completada' %>
          <a href="/tasks/<%= task.id %>/editar">Editar</a>
        </li>
    <% }); %>
  </ul>
</body>
</html>